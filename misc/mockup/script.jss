// script.js
function toggleTheme() {
    // Toggle between dark and light themes (you can customize this logic)
    document.body.classList.toggle("dark-theme");
}

// You can add more logic here to persist the user's theme preference
// (e.g., using local storage)
